# TTMediaBot — Russian Edition

Привет! Меня зовут **DJ Dancho**, и это мой форк [TTMediaBot](https://github.com/gumerov-amir/TTMediaBot) — медиа-бот для TeamTalk 5, переделанный специально для тех, у кого серверы в России.

> Оригинальный проект: [gumerov-amir/TTMediaBot](https://github.com/gumerov-amir/TTMediaBot)
> Мой репозиторий: [Daket52i/TTMediaBot-by-dancho](https://github.com/Daket52i/TTMediaBot-by-dancho)

## Что изменено

- **Русские сервисы:** Rutube (видео), Музфонд и Хитмо (музыка) — работают без VPN и блокировок
- **YouTube убран полностью:** из России он не работает, поэтому сервис отключён, а из установщика убраны Node.js и запрос cookies.txt
- **Убраны лишние сервисы:** Yandex Music и VK удалены (можно вернуть при необходимости)
- **TeamTalk SDK 5.22a:** библиотека скачивается установщиком под архитектуру сервера
- **Починены источники:** Музфонд переехал на muzofond.su и отдаёт mp3 напрямую, скачивание проверяется на пустой файл и HTML вместо музыки
- **Автозапуск и рестарт:** настроен systemd-сервис + ежедневный рестарт в 3:00 ночи

## Сервисы

| Сервис | Команда | Описание |
|--------|---------|----------|
| **Музфонд** | `sv mf` | Музыка, mp3 (основной сервис) |
| **Хитмо** | `sv hitmo` | Музыка, mp3 |
| **Rutube** | `sv rt` | Видео с Rutube (API, без авторизации) |

Переключение: отправьте боту `sv mf`, `sv hitmo` или `sv rt`.

## Установка

### Быстрый старт (рекомендуется)

```bash
wget https://raw.githubusercontent.com/Daket52i/TTMediaBot-by-dancho/master/install_git_clone.sh
chmod +x install_git_clone.sh
sudo ./install_git_clone.sh
```

Скрипт сам установит зависимости (Git, Docker, ffmpeg), склонирует репозиторий и запустит менеджер ботов.

### Альтернатива — локальная установка (без Docker)

```bash
git clone https://github.com/Daket52i/TTMediaBot-by-dancho.git
cd TTMediaBot-by-dancho
sudo ./install.sh
```

Установит Python venv, Node.js зависимости, ffmpeg и TeamTalk библиотеки напрямую на сервер.

## Конфигурация

1. Скопируйте шаблон конфига:
   ```bash
   cp config_default.json config.json
   ```

2. Отредактируйте `config.json` — заполните данные вашего TeamTalk сервера:
   ```json
   {
       "teamtalk": {
           "hostname": "ваш-сервер.ru",
           "tcp_port": 10333,
           "udp_port": 10333,
           "username": "bot",
           "password": "ваш_пароль",
           "channel": "/Канал"
       }
   }
   ```

3. Сервисы `rt` и `mf` уже добавлены в `config_default.json` — просто скопируйте и отредактируйте.

## Команды бота

Отправляйте команды боту в личные сообщения или в канал.

### Основные

| Команда | Аргументы | Описание |
|---------|-----------|----------|
| `h` | | Показать справку по командам |
| `p` | `[запрос]` | Искать и играть. Без аргумента — пауза/продолжить |
| `u` | `[url]` | Играть по ссылке |
| `s` | | Остановить воспроизведение |
| `n` | `[номер/?]` | Следующий трек. `n ?` — показать позицию в очереди |
| `b` | | Предыдущий трек |
| `v` | `[0-100]` | Установить громкость. Без аргумента — показать текущую |
| `sb` | `[секунды]` | Перемотка назад (по умолчанию — шаг из настроек) |
| `sf` | `[секунды]` | Перемотка вперёд |
| `c` | `[номер/?]` | Перейти к треку по номеру. `c ?` — текущая позиция |
| `m` | `[режим]` | Режим: `SingleTrack`, `RepeatTrack`, `TrackList`, `RepeatTrackList`, `Random` |
| `sp` | `[0.25-4]` | Скорость воспроизведения |
| `sv` | `[сервис]` | Переключить сервис: `sv yt`, `sv ytm`, `sv rt`, `sv mf` |
| `f` | | Управление избранным. `f` — список, `f +` — добавить, `f -` — удалить, `f [номер]` — воспроизвести |
| `gl` | | Получить ссылку на текущий трек |
| `dl` | | Скачать текущий трек и загрузить в канал |
| `dlv` | | Скачать текущий трек как видео |
| `dlp` | `[url]` | Скачать весь плейлист/альбом по ссылке |
| `r` | `[номер]` | Недавние. `r` — список, `r [номер]` — воспроизвести |
| `jc` | | Заставить бота зайти в ваш текущий канал |
| `qa` | `[запрос]` | Добавить трек в очередь |
| `ql` | | Показать очередь |
| `qr` | `[номер]` | Удалить трек из очереди |
| `qc` | | Очистить очередь |
| `qs` | | Пропустить текущий трек из очереди |
| `sr` | `[on/off]` | Режим результатов поиска (показывает список вместо воспроизведения) |
| `sl` | `[номер]` | Выбрать трек из списка результатов |
| `slc` | `[число]` | Количество результатов в режиме `sr` |
| `a` | | Информация о боте |

### Скачивание по ссылкам

| Команда | Аргументы | Описание |
|---------|-----------|----------|
| `aad` | `[ссылка]` | Добавить ссылку в список скачивания |
| `ad` | `[ссылки]` | Добавить несколько ссылок через пробел |
| `ld` | | Показать список ссылок |
| `rd` | `[номер/ссылка]` | Удалить ссылку из списка |
| `ldd` | `[ссылка]` | Скачать ссылку сразу и загрузить в канал |
| `ads` | `[1/2]` | Скачать список: 1 — по очереди, 2 — ZIP-архив |
| `adsc` | | Переключить локальный режим (сохранять на сервер вместо загрузки в канал) |

### Админские команды

Требуют привилегий админа (настраиваются в `config.json` → `teamtalk.users.admins`).

| Команда | Аргументы | Описание |
|---------|-----------|----------|
| `cg` | `[n/m/f]` | Изменить пол бота |
| `cl` | `[код]` | Изменить язык (`ru`, `en`, `es`, `pt_BR` и др.) |
| `cn` | `[имя]` | Изменить никнейм |
| `cs` | `[текст]` | Изменить статус |
| `cc` | `[r/f]` | Очистить кэш (`r` — недавние, `f` — избранное) |
| `cm` | | Вкл/выкл отправку сообщений в канал |
| `ajc` | `[id] [пароль]` | Принудительно зайти в канал по ID |
| `bc` | `[+/-команда]` | Заблокировать/разблокировать команду |
| `l` | | Заблокировать/разблокировать бота (только админы) |
| `ua` | `[+/-user]` | Добавить/удалить админа |
| `ub` | `[+/-user]` | Заблокировать/разблокировать пользователя |
| `eh` | | Вкл/выкл внутреннюю обработку событий |
| `sc` | | Сохранить конфигурацию в файл |
| `va` | | Вкл/выкл передачу голоса |
| `rs` | | Перезапустить бота |
| `q` | | Остановить бота |
| `gcid` | | Получить ID текущего канала |

## Автозапуск и рестарт

Если установка была через Docker — автозапуск настроен автоматически.

Для ручной установки (systemd):
```bash
# Сервис: /home/bot/.config/systemd/user/TTMediaBot.service
# Автозапуск: loginctl enable-linger bot
# Ежедневный рестарт в 3:00: ttbot-restart.timer
```

Проверить статус:
```bash
su - bot -c 'XDG_RUNTIME_DIR=/run/user/1002 systemctl --user status TTMediaBot'
```

## Cookie для YouTube

Для работы YouTube/YouTube Music нужен файл `cookies.txt`:

1. Установите расширение [Get cookies.txt LOCALLY](https://chrome.google.com/webstore/detail/get-cookiestxt/bgaddhkoddajcdgocldbbfleckgcbcid) в браузере
2. Зайдите на youtube.com
3. Экспортируйте cookies через расширение
4. Положите файл на сервер и укажите путь в `config.json` → `services.yt.cookiefile_path`

> Rutube и Музфонд работают **без cookies**.

## Языки

Бот поддерживает: `ru`, `en`, `es`, `pt_BR`, `ar`, `tr`, `hu`, `id`

Смена языка: `cl ru`

## Лицензия

MIT License — см. [LICENSE](LICENSE)

---

Создано DJ Dancho для русскоязычного сообщества TeamTalk. Вопросы и идеи — в [Issues](https://github.com/Daket52i/TTMediaBot-by-dancho/issues).
